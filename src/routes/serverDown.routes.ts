// import { Router } from "express";


// //Defining Router Object
// const router = Router();
// router.route('/').post(toggleServerHandler);
// // serverRouter.post(`${baseURL}/serverdown`, toggleServerHandler);

// export default router;
